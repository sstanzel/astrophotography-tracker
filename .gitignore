# --- Per-machine (start from templates/config.example.toml) ---
config.toml

# --- Derived / generated artifacts (regenerated by the pipeline) ---
tracker.db
tracker.db-journal
tracker.db-wal
Astrophotography tracker (generated).xlsx
tracker_dashboard.html

# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/

# --- macOS / editor cruft ---
.DS_Store
.Rhistory
node_modules/
package.json
package-lock.json
.claude/
.venv/

# --- Office lock files (workbook open in Excel) ---
~$*
